reg = require('./reg')
auth = require('./auth')
logout = require('./logout')
main = require('./main')
sign_in = require('./sign_in')
addTask = require('./addTask')

module.exports = {
    reg,
    auth,
    logout,
    main,
    sign_in,
    addTask
}